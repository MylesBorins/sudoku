'use strict';
var generate = require('./generate');
var check = require('./check');
var template = require('./template');

module.exports = {
  generate: generate,
  template: template,
  check: check
};
